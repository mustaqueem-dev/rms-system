{
  "name": "@rms/event-contracts",
  "version": "1.0.0",
  "private": true,
  "description": "Strongly-typed Kafka event payload contracts shared across all RMS microservices",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build":     "tsc --project tsconfig.json",
    "typecheck": "tsc --noEmit",
    "lint":      "eslint 'src/**/*.ts'"
  },
  "dependencies": {
    "@rms/shared-kernel": "*"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
